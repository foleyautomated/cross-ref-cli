# Core dependencies for cross-ref-cli

# Transformers for NLI models (also used by sentence-transformers)
transformers>=4.30.0
torch>=2.0.0

# Sentence transformers for embeddings
sentence-transformers>=2.2.0

# FAISS for vector similarity search
faiss-cpu>=1.7.4

# Alternative for GPU support (uncomment if needed):
# faiss-gpu>=1.7.4

# For text processing
numpy>=1.24.0

# For CSV output
pandas>=2.0.0

# Progress bars
tqdm>=4.65.0
